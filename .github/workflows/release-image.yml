name: Release RAVE OS image

on:
  workflow_dispatch:
  push:
    tags: ['v*']

permissions:
  contents: write

jobs:
  build-package:
    runs-on: ubuntu-24.04-arm
    steps:
      - uses: actions/checkout@v4
      - name: Build Debian package
        run: |
          echo "TODO: build signed arm64 rave-edge package"
          exit 1

  build-image:
    needs: build-package
    runs-on: [self-hosted, linux, arm64, rave-image-builder]
    steps:
      - uses: actions/checkout@v4
      - name: Build reproducible RAVE OS image
        run: |
          echo "TODO: invoke pinned rpi-image-gen build"
          exit 1
      - name: Hardware smoke test
        run: |
          echo "TODO: flash/test on Pi 5 + Hailo + approved camera"
          exit 1
      - name: Publish signed release artifacts
        run: |
          echo "TODO: checksum, sign, and upload release assets"
          exit 1
